#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
*/
void print_alphabet(void);
int main(void)
{
	char alphabet;

	for (n = 'a'; n <= 'z'; n++)
	{
		_putchar(n);
	}
	
	_putchar('\n');
}
